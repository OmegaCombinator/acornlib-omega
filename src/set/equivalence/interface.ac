/// Public interface for the package.

from basic.relation_basic import is_equivalence, is_reflexive, is_symmetric,
    is_transitive
from basic.relation_transport import respects_unary_op, respects_binary_op,
    respects_function, respects_predicate, is_unary_congruence,
    is_binary_congruence
from basic.functions import Inhabited, is_surjective_fn
from set.set import Set, equivalence_class, is_saturated_under
