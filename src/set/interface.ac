/// Public interface for the package.

from relation import is_equivalence, is_reflexive, is_symmetric,
    is_transitive, respects_unary_op, respects_binary_op, respects_function,
    respects_predicate, is_unary_congruence, is_binary_congruence
from function import Inhabited, is_surjective_fn
from order import PartialOrder, LinearOrder
from number_theory import closed_interval, open_interval, left_open_interval,
    right_open_interval
from function import compose, identity_fn, is_monotone, is_antitone,
    is_strict_monotone, is_strict_antitone
from nat import Nat, eventually_predicate_nat
from function import is_order_embedding, is_lower_bound, is_upper_bound,
    is_least, is_greatest, is_glb, is_lub, is_bounded_below,
    is_bounded_above
from pair import pair_map
from algebra import Pair
from nat import is_bijection_fn, eq_relation, relation_compose,
    relation_converse, relation_subset, relation_symmetric_closure,
    relation_pullback
from function import is_injective_fn, Bijection, is_antisymmetric,
    is_irreflexive, is_asymmetric, is_total
from nat import List, map
from function import inverse_fn, is_right_inverse_fn
from list import sum, is_partial_equivalence, respects_equivalence
from order import relation_pushforward

numerals Nat
export order_relation
export set
export set_lattice
export witness

