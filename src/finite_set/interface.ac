/// Public interface for the package.

from nat import Nat
from list import List
from basic.functions import Inhabited, compose, identity_fn, inverse_fn,
    is_bijection_fn, is_injective_fn
from order import LinearOrder, PartialOrder
from set.set import Set, list_set, set_image
from set.witness import exists_unique, set_witness_predicate,
    choose_from_set_or_default
from list import map
from set.set_list_partition import set_list_union, set_list_exactly_covers,
    set_list_pairwise_disjoint, set_list_partition_of,
    set_list_all_cardinality_is, set_list_all_cardinality_by,
    set_list_cardinality_sum
from list import sum, is_permutation
from pair import Pair
from set.pair_set import pair_set
from algebra_core import AddCommMonoid, add_fn, mul_fn, Semiring
from combinatorics import is_partition, is_partition_sum, partition_set,
    partition_set_contains_iff, p, partition_four_char, p4_a, p4_b, p4_c,
    p4_d, p4_e, p4_sound, pair_is_unique, cons_neq_head, bool_eq_of_iff,
    contains_le_sum, filter_all_keep, p_one, p_two, p_three, p_four,
    partition_set_finite, is_partition_all_positive,
    is_partition_non_increasing, is_partition_intro, all_positive,
    all_positive_head, all_positive_tail, non_increasing,
    non_increasing_from, non_increasing_second, non_increasing_tail, p_zero,
    partition_three_char, triple_is_unique, quad_is_unique,
    cons_cons_neq_head, sum_ge_length_all_positive, zero_lte, lt_two_cases,
    lt_three_cases, lt_four_cases, ni_single, ni_21, ni_31, ni_22, ni_111,
    ni_211, ni_1111, ap_single, ap_21, ap_31, ap_22, ap_111, ap_211, ap_1111,
    is_odd, is_odd_suc, is_odd_zero, is_odd_one, is_odd_two, is_odd_three,
    is_odd_four, all_odd, is_odd_part_partition, is_distinct_part_partition,
    odd_partition_set, distinct_partition_set,
    odd_partition_set_contains_iff, distinct_partition_set_contains_iff,
    all_odd_31, all_odd_1111, all_odd_4, all_odd_22, all_odd_211,
    pair_nat_unique, not_unique_22, not_unique_211, not_unique_1111,
    p_grows_one_two, p_grows_two_three, p_grows_three_four

numerals Nat
