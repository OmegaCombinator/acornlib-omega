/// Public interface for the package.

from relation import is_reflexive, is_transitive, is_antisymmetric
from function import compose, identity_fn, is_injective_fn, is_surjective_fn,
    Inhabited, Bijection, inverse_fn, is_bijection_fn
from relation import eq_relation, is_asymmetric, is_equivalence,
    is_irreflexive, is_partial_equivalence, is_symmetric, is_total,
    relation_converse, relation_intersection, relation_reflexive_closure,
    relation_subset, relation_symmetric_closure, relation_union
from algebra_core import Add, Mul

export order
export order_bounds
export relation_transport

