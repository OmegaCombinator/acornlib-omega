/// Public interface for the package.

from algebra_core import Add, Mul, PartialOrder, LinearOrder, is_monotone,
    is_order_embedding, is_strict_monotone, Inhabited, LTE, is_reflexive,
    is_transitive, is_antisymmetric, Zero, One
from algebra import Pair
from order import predicate_subset
from algebra_core import CommMonoid, Monoid, MonoidHom, Semigroup,
    AddSemigroup, AddCommSemigroup, AddMonoid, AddCommMonoid, Semiring,
    CommSemigroup
from order import Meet, Join, MeetSemilattice, JoinSemilattice, Lattice,
    DistribLattice
from algebra_core import mul_fn, add_fn

export multiplication
export nat_base
export nat_square

