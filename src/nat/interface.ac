/// Public interface for the package.

from algebra_core import Add, Mul
from function import Inhabited
from order import PartialOrder, LinearOrder, is_monotone, is_order_embedding,
    is_strict_monotone, LTE
from relation import is_reflexive, is_transitive, is_antisymmetric
from algebra_core import Zero, One
from pair import Pair
from order import predicate_subset
from algebra_core import CommMonoid, Monoid, MonoidHom, Semigroup,
    AddSemigroup, AddCommSemigroup, AddMonoid, AddCommMonoid, Semiring,
    CommSemigroup
from order import Meet, Join, MeetSemilattice, JoinSemilattice, Lattice,
    DistribLattice
from algebra_core import add_fn, mul_fn

export multiplication
export nat_base
export nat_square

